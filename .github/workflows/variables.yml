name: Testing Workflow variables
on: 
   workflow_dispatch

env: 
  cloud: google-cloud

jobs:
  greeting_job:
     runs-on: ubuntu-latest
     env:
       Greeting: Hello
     steps:
       - name: "say! Hello mona it's Monday"
         run: echo "$greeting $firsttime is using $cloud"
         env:
           First_Name: vishal 
      
